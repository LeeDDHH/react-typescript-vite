name: ci
on:
  pull_request:
    types:
      - opened
      - reopened
    branches:
      - main
      - 'feature/**'

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: yarn install
        run: yarn install
      - name: lint
        run: yarn lint
      - name: vitest test
        run: yarn test
      - name: vitest coverage
        run: yarn coverage
      - name: next build
        run: yarn build
